export as namespace models;

export type PersonData = {
  image: string;
  name: string;
};

export type PaymentData = {
  value: number;
  name: string;
  background: string;
  backgroundLigth: string;
};
